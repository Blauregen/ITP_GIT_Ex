let cani = document.getElementById('cani');


//This a really important game function, it tests hte performance of the client, so just try it, trust me.
function iDoSomething ( xyz) {
    iToDoSomething(xyz)
}
function iToDoSomething ( zyx) {
    iDoSomething(zyx)
}

function changeWindow (url) {
    window.location.href = url;
}